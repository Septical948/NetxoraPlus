<Window x:Class="DBACheck2.App.AlertInboxWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="DBACHECK 2 - Alert Inbox" Background="#0A0D18">
<Grid Margin="12">
 <Grid.RowDefinitions>
  <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
  <RowDefinition x:Name="ListRow" Height="*"/><RowDefinition Height="Auto"/><RowDefinition x:Name="DetailRow" Height="125"/>
 </Grid.RowDefinitions>

 <StackPanel Margin="0,0,0,12">
  <TextBlock x:Name="TitleText" Text="ALERT INBOX" Foreground="#38E8D0" FontSize="24" FontWeight="Bold"/>
  <TextBlock x:Name="SubtitleText" Text="Cross-monitor operational queue · No database connection required to load alerts" Foreground="#B7C3D7"/>
 </StackPanel>

 <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,12">
  <Border Background="#121427" BorderBrush="#302B4E" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,8,0">
   <StackPanel><TextBlock Text="P1" Foreground="#8F9BB3" FontWeight="Bold"/><TextBlock x:Name="P1Text" Text="0" Foreground="#FF6B7A" FontSize="20" FontWeight="Bold"/></StackPanel>
  </Border>
  <Border Background="#121427" BorderBrush="#302B4E" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,8,0">
   <StackPanel><TextBlock Text="P2" Foreground="#8F9BB3" FontWeight="Bold"/><TextBlock x:Name="P2Text" Text="0" Foreground="#F0B45A" FontSize="20" FontWeight="Bold"/></StackPanel>
  </Border>
  <Border Background="#121427" BorderBrush="#302B4E" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,8,0">
   <StackPanel><TextBlock x:Name="OpenLabel" Text="ACTIVE GROUPS" Foreground="#8F9BB3" FontWeight="Bold"/><TextBlock x:Name="OpenText" Text="0" Foreground="#E8EDF7" FontSize="20" FontWeight="Bold"/></StackPanel>
  </Border>
  <Border Background="#121427" BorderBrush="#302B4E" BorderThickness="1" CornerRadius="8" Padding="10">
   <StackPanel><TextBlock x:Name="UnmatchedLabel" Text="UNMATCHED" Foreground="#8F9BB3" FontWeight="Bold"/><TextBlock x:Name="UnmatchedText" Text="0" Foreground="#BDA7FF" FontSize="20" FontWeight="Bold"/></StackPanel>
  </Border>
 </UniformGrid>

 <Grid Grid.Row="2" Margin="0,0,0,10">
  <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
  <Button x:Name="RefreshButton" Content="REFRESH ALL MONITORS" Padding="15,7" MinWidth="175" Click="RefreshButton_Click"/>
  <Button Grid.Column="1" x:Name="DiagnoseButton" Content="DIAGNOSE SELECTED" Padding="15,7" MinWidth="155" Margin="8,0,0,0" IsEnabled="False" Click="DiagnoseButton_Click"/>
  <Button Grid.Column="2" x:Name="CreateIncidentButton" Content="CREATE INCIDENT" Padding="15,7" MinWidth="135" Margin="8,0,0,0" IsEnabled="False" Click="CreateIncidentButton_Click"/>
  <TextBlock Grid.Column="3" x:Name="StatusText" Foreground="#D5DEEB" Margin="14,7,0,0" TextWrapping="Wrap"/>
 </Grid>

 <DataGrid x:Name="InboxGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
  Background="#111C2F" Foreground="White" RowBackground="#111C2F" AlternatingRowBackground="#16243A"
  SelectionChanged="InboxGrid_SelectionChanged">
  <DataGrid.Columns>
   <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="70"/>
   <DataGridTextColumn Header="State" Binding="{Binding State}" Width="90"/>
   <DataGridTextColumn Header="Host" Binding="{Binding Host}" Width="150"/>
   <DataGridTextColumn Header="Env" Binding="{Binding Environment}" Width="70"/>
   <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="115"/>
   <DataGridTextColumn Header="Sources" Binding="{Binding Sources}" Width="145"/>
   <DataGridTextColumn Header="Alerts" Binding="{Binding AlertCount}" Width="55"/>
   <DataGridTextColumn Header="Age" Binding="{Binding AgeText}" Width="75"/>
   <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="*"/>
   <DataGridTextColumn Header="Profile" Binding="{Binding ProfileName}" Width="125"/>
  </DataGrid.Columns>
 </DataGrid>

 <Grid Grid.Row="4" Margin="0,10,0,6">
  <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
  <TextBlock x:Name="DetailTitleText" Text="PRIORITY / CORRELATION DETAIL" Foreground="#38E8D0" FontWeight="Bold"/>
  <Button Grid.Column="1" x:Name="ExpandDetailButton" Content="EXPAND" Padding="12,4" Click="ExpandDetailButton_Click"/>
 </Grid>

 <TextBox x:Name="DetailText" Grid.Row="5" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="#111C2F" Foreground="#E8EDF7"
  BorderThickness="0" Padding="12" FontFamily="Consolas"
  Text="Alert Inbox loads monitoring alerts first. Database collectors run only when you choose DIAGNOSE SELECTED."/>
</Grid>
</Window>